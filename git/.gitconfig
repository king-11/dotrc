[user]
	name = Lakshya Singh
	email = lakshay.singh1108@gmail.com
[core]
	editor = nvim
	username = Lakshya Singh
	name = Lakshya Singh
[help]
	autocorrect = 100
[alias]
	co = checkout
	ci = commit
	ca = commit -v --amend
	br = branch
	bd = branch -D
	type = cat-file -t
	pretty = cat-file -p
	lg = log --graph
	ol = log --pretty=oneline --graph --decorate --all
	ad = add -p
	aa = add .
	dfs = diff --staged
	df = diff
	last = log -1 HEAD
	pu = pull upstream
	ra = rebase --abort
	rc = rebase --continue
	pof = push origin --force-with-lease
	po = push origin
	rs = reset
	rhh = reset --hard HEAD^
	rh = reset --hard
	s = status -s
	
[commit]
	template = ~/scripts/git/gitmessage
[pull]
	ff = only
[init]
	defaultBranch = main
[color]
	ui = auto
[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
